spring:
  jpa:
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQL8Dialect
        format_sql: 'TRUE'
    hibernate:
      ddl-auto: update
    open-in-view: 'false'
    show-sql: true
  sql:
    init:
      mode: never


  datasource:
    driverClassName: com.mysql.cj.jdbc.Driver
    username: root
    password: 0887149505Yoan!

    url: jdbc:mysql://localhost:3306/reservations?allowPublicKeyRetrieval=true&useSSL=false&createDatabaseIfNotExist=true&serverTimezone=UTC
logging:
  level:
    org:
      hibernate:
        type:
          descriptor: TRACE
        SQL: DEBUG
      nodeValue: WARN
    blog: WARN
server:
  port: 8081
